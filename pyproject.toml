[project]
name = "secondgen_autoenc"
version = "0.1.0"
description = ""
authors = [
    {name = "Skye Sprung",email = "skye.sprung@gmail.com"}
]
requires-python = ">=3.12.0,<=3.13.20"
dependencies = [
    "torch (==2.14.0)",
    "torchvision (==0.29.0)",
    "numpy (==2.5.2)",
    "matplotlib (>=3.11.0,<3.12.0)",
    "tensorboard (>=2.21.0, <2.22.0)",
    "wget (>=3.2, <3.3)",
    "requests (>=2.34.0, <2.35)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
